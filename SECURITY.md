# 보안 정책 (Security Policy)

EASYREAD 서버의 보안 취약점을 책임감 있게 신고하는 방법을 안내합니다.

## 지원 버전

| 버전 | 지원 |
|---|---|
| 0.1.x | ✅ (초기 개발판) |

첫 공개 릴리스(`v0.1.0`) 전까지는 최신 커밋을 기준으로 대응합니다.

## 취약점 신고 방법

**공개 이슈로 올리지 마세요.** 보안 문제는 비공개로 알려 주세요.

1. 이 저장소의 **Security 탭 → “Report a vulnerability”**(GitHub 비공개 보안 신고)를 이용해 주세요.
2. 다음 내용을 적어 주시면 빠르게 확인할 수 있습니다.
   - 어떤 문제인지, 어떻게 재현하는지
   - 영향(무엇이 가능해지는지)
   - 사용한 버전과 실행 환경(OS, Node.js 버전)

접수 후 **7일 안에** 확인 회신을 드리고, 수정 방향과 일정을 함께 알려 드립니다.

## 이 서버의 보안 특성 (공격 표면이 작은 이유)

이 프로젝트는 설계상 위험 표면이 작습니다.

- **네트워크를 호출하지 않습니다** (NFR-01). 서버는 오프라인에서 결정적으로 동작하며, 리소스의 URL도 실행 중에 가져오지 않습니다. 참조용 값입니다.
- **입력을 저장·전송·로깅하지 않습니다** (NFR-03). 표준 오류(stderr) 로그에도 입력 본문을 남기지 않습니다. 사용 현황 텔레메트리도 수집하지 않습니다.
- **입력 크기에 상한**이 있습니다. `validate_easy_read`·`analyze_readability`는 50,000자, `lookup_easy_word`는 낱말 50자로 제한하고, 위반 항목 개수도 절단해 과도한 입력으로 인한 자원 소모를 막습니다.
- **규칙 정규식은 단순·경계형**입니다. 중첩 수량자 등 파국적 역추적(ReDoS) 패턴을 쓰지 않습니다.
- **런타임 의존성이 2개뿐**입니다(`@modelcontextprotocol/sdk`, `zod`). 공급망 표면을 최소로 유지하며, 잠금 파일(`package-lock.json`)을 커밋하고 `npm audit`를 CI에서 확인합니다.

## 범위

이 서버는 **감수를 대체하지 않는 초안 보조 도구**입니다. 변환 결과의 사실 정확성(날짜·금액·기관명 등)은 당사자 감수로 확정해야 합니다. 규칙의 오탐·미탐은 보안 문제가 아니라 품질 이슈이며, [오탐·미탐 신고 템플릿](.github/ISSUE_TEMPLATE/false-detection.yml)으로 알려 주세요.
